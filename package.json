{
  "name": "network-system",
  "version": "1.0.0",
  "description": "Network System - Fullstack Application",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install:all": "npm install && cd client && npm install",
    "build:client": "cd client && npm run build",
    "deploy": "npm run build:client && gh-pages -d client/dist",
    "start:server": "node server/app.js",
    "dev": "concurrently \"npm run start:server\" \"cd client && npm run dev\""
  },
  "keywords": ["network", "system", "react", "express"],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "concurrently": "^8.2.2",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "gh-pages": "^6.3.0",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.19.0",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  }
}
